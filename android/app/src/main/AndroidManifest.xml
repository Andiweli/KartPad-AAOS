<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" tools:node="remove" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:node="remove" />

    <uses-feature android:glEsVersion="0x00030000" android:required="false" />
    <uses-feature android:name="android.hardware.vulkan.level" android:required="false" android:version="1" />
    <uses-feature android:name="android.hardware.vulkan.version" android:required="false" android:version="0x00401000" />

    <!-- Optional inputs avoid filtering out cars without these feature declarations. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <application
        android:name=".KartPadApplication"
        android:resizeableActivity="true"
        android:appCategory="game"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:theme="@style/KartPadTheme">
        <!-- Opt-in local timing/call-stack capture; never enables debugging. -->
        <profileable android:shell="${kartpadProfileable}" tools:targetApi="29" />
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            tools:node="remove" />
        <activity
            android:name=".KartPadPerformanceExportActivity"
            android:exported="false"
            android:process=":launcher"
            android:screenOrientation="${kartpadOrientation}" />
        <activity
            android:name=".RetroRewindInstallActivity"
            android:exported="false"
            android:screenOrientation="${kartpadOrientation}" />
        <activity
            android:name=".KartPadGameDataActivity"
            android:exported="false"
            android:process=":launcher"
            android:screenOrientation="${kartpadOrientation}" />
        <activity
            android:name=".KartPadActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="${kartpadOrientation}" />
        <activity
            android:name=".KartPadPausedMenuActivity"
            android:exported="false"
            android:screenOrientation="${kartpadOrientation}" />
        <activity
            android:name=".KartPadLaunchActivity"
            android:exported="true"
            android:process=":launcher"
            android:screenOrientation="${kartpadOrientation}">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
